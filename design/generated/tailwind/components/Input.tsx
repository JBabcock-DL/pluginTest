// Generated by FigHub — Input (react). Do not edit by hand.
// Tailwind utility classes reference the pushed tailwind v4 design tokens.
import * as React from 'react';

export interface InputProps
  extends React.ComponentPropsWithoutRef<'div'> {
  leadingIcon?: boolean;
  placeholder?: string;
  readOnly?: boolean;
  label?: React.ReactNode;
}

// NOTE: one representative utility per style category. Per-variant styling (a cva()
// variant map) needs per-variant binding data the flat spec omits — the FigHub agent
// layer (WO-126) refines this into the full variant matrix.
const rootClass = 'inline-flex items-center rounded-md border-destructive px-5 py-1.5';
const labelClass = 'text-foreground';

export function Input({ leadingIcon, placeholder, readOnly, label = 'Input', className, ...rest }: InputProps) {
  return (
    <div
      className={[rootClass, className].filter(Boolean).join(' ')}
      {...rest}
    >
      {label != null ? <span className={labelClass}>{label}</span> : null}
    </div>
  );
}
